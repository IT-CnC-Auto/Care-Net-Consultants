# HSF FORGE build contract (portal, builder, uploads, MCO transfer, kernel API)

Version 1.0 | 23/09/2026 | Binding on every builder. Where this contract and SPEC.md Part B differ, this contract wins for the items it names and SPEC.md Part B wins for everything else. House rules from SPEC.md B1.5 apply to every visible string: SA British English, rand as R4 250,00, no dash or hyphen punctuation in prose (identifiers, file names, URLs and official names excepted), staff are sales executives, numbering from 1, no "compliant" stamps of any kind (never "POPIA compliant"), Care Net screens fitness and does not diagnose.

## 1. Constants

| Name | Value |
| --- | --- |
| Supabase URL | `https://pboebfnujzffgwctsplw.supabase.co` |
| Publishable (anon) key, public by design | `sb_publishable_E6ebjt_HJy5RN6MzM6fsdg_I1VbB1aj` |
| Server secrets (env only, never in files) | `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `MCO_BASE_URL`, `MCO_API_TOKEN` (the last two pending HSF-3) |
| WhatsApp (the only verified number) | `27600702723`, shown as `27 60 070 2723`, link `https://wa.me/27600702723` |
| Main site | `https://www.carenetconsultants.co.za` ; privacy policy `https://www.carenetconsultants.co.za/privacy-policy` |
| Google Tag Manager ID | NOT KNOWN. `null` in config, marked pending. Never invent one. |
| MCO portal / SSO URL | NOT KNOWN (HSF-3). `null` in config. |
| Staging bucket | `hsf-staging` (private). Object path: `<client_account_id>/<upload_id>/<safe_file_name>` |
| Consent wording version | `HSF-CONSENT-1.0` |
| Upload limits | 25 MB per file (26214400 bytes); types: application/pdf, image/jpeg, image/png, application/vnd.openxmlformats-officedocument.wordprocessingml.document, application/vnd.openxmlformats-officedocument.spreadsheetml.sheet, application/msword, application/vnd.ms-excel, text/csv. Held in msp_env_parameter (`hsf.upload_max_bytes`, `hsf.upload_allowed_mime`), not in code. |
| Existing auth | Supabase Auth, email one time link (see vercel/index.html). Client contact row: `msp_client_account.auth_user_id`. |

"Supabase Secrets" in the Director's brief means the secure Supabase environment. Files go to Supabase **Storage** (private bucket, encrypted at rest, service role access only), never to the Secrets store, which holds credentials.

## 2. Departments (company departments a file is filed under)

Table `hsf_department (code text pk, name text, ordinal int)`, seeded:
EXEC Executive and legal; HR Human resources; SHE Health and safety; OPS Operations; ENG Engineering and maintenance; PROC Procurement and contractors; OH Occupational health and medical; TRAIN Training and development; FAC Facilities and security.
Every upload carries a `department_code` and a File `section_code` (A to O) and optionally an `element code`.

## 3. Database (migrations, in order; all must replay cleanly with test/sql/replay.sh)

**047_hsf_core_schema.sql.** Tables of SPEC.md B4.3 to B4.5 exactly as written there, in dependency order (hsf_appointment_type before hsf_element), with these deltas:
- `hsf_trigger (code text pk, description text)`; `hsf_element.trigger_code` references it (nullable).
- `hsf_department` as section 2.
- `hsf_evidence`: `storage_path` nullable; add `upload_id uuid` (FK to hsf_upload, added in 049 via alter), `mco_document_ref text`, `transferred_at timestamptz`, `staging_deleted_at timestamptz`. The append only trigger permits exactly these one way updates: set `revoked_at` once; set `mco_document_ref`, `transferred_at` once; set `staging_deleted_at` once together with `storage_path` to null. Everything else refused; delete refused.
- `hsf_file.reference` generated by `hsf_next_reference()` (CNC-HSF-YYYY-MMDD-NNN, gap safe with advisory lock, same pattern as msp_next_reference in migration 041).
- `msp_audit` gains `hsf_file_id uuid references hsf_file(id)` plus index (SPEC B4.9).
- `msp_legal_instrument.scope` and `msp_industry_instrument.scope` (SPEC B5.1, B5.2).
- Role forge_safety_reviewer is a JWT app_metadata role like the others; no DB role needed. Use `msp_has_role(text)` from migration 002.
- RLS on every table. Library tables: select for authenticated; no writes except service role. Engagement tables: client reads own rows where `client_account_id` belongs to `auth.uid()` (join msp_client_account.auth_user_id); staff read with forge_admin, forge_omp, forge_safety_reviewer; writes only through security definer functions. Revoke all from anon and public on every table.

**048_hsf_library_seed.sql.** Generated by `hsf/build_seed.py` from SPEC.md Part B (B6 universal elements, B6.3.1 appointment types, B6.5.1 courses, B6.5.2 licences, B7 overlays, B9.2 triggers) and the kernel pack protocols (as hsf/build_samples.py does). Inserts sections A to O, departments, triggers, appointment types, elements (basis_state 'awaiting' for all), element industry rows, and **candidate instruments as status 'pending'** (scope 'safety') for every Section 5 candidate named in the element library that the kernel does not already hold, with source_one/two/three set to the text 'Gate a pending', 'Gate b pending', 'Gate c pending'. `hsf_element_instrument` links elements to instruments (held ones by short_name lookup; provision 'awaiting verification'). Idempotent (on conflict do nothing / where not exists). Must not change any existing instrument row. **Frozen (24/09/2026):** 048 is applied to the live project, so `hsf/build_seed.py` pins its SHA-256 and never writes it again. It checks that SPEC Part B still renders everything outside the element rows exactly as 048 loaded it, turns each element row change into an idempotent update (`python3 hsf/build_seed.py --delta`) that a new migration must carry (the first is 056, HSF-E-01), and refuses any other SPEC change until a migration and a check are written for it. `test/api/hsf-seed.test.js` runs the check in every `node --test`.

**049_hsf_consent_uploads_transfer.sql.**
- `hsf_consent (id uuid pk, client_account_id uuid not null fk, auth_user_id uuid not null, consent_kind text check in ('document_storage','mco_transfer','authority_to_share'), granted boolean not null, wording_version text not null, granted_at timestamptz not null default now(), withdrawn_at timestamptz)`.
- `hsf_upload (id uuid pk, client_account_id uuid not null fk, auth_user_id uuid not null, file_id uuid fk null, file_item_id uuid fk null, section_code text fk hsf_section null, department_code text fk hsf_department not null, original_name text not null, safe_name text not null, mime_type text not null, size_bytes bigint not null check (> 0), sha256_client text not null check hex64, sha256_server text, storage_bucket text not null default 'hsf-staging', storage_path text, status text not null check in ('awaiting_upload','uploaded','verified','held','transferring','transferred','staging_deleted','rejected','failed') default 'awaiting_upload', reject_reason text, mco_document_ref text, created_at, uploaded_at, verified_at, transferred_at, staging_deleted_at)`.
- `hsf_mco_transfer (id bigint identity pk, upload_id uuid fk not null, mode text check in ('hold','fixture','live'), outcome text check in ('held','received','hash_mismatch','error'), mco_document_ref text, mco_receipt_sha256 text, error text, created_at)`; append only (trigger refuses update and delete).
- Bucket: `insert into storage.buckets (id, name, public, file_size_limit, allowed_mime_types) values ('hsf-staging','hsf-staging',false,26214400, array[...]) on conflict do nothing`. No storage.objects policy grants anything on this bucket to anon or authenticated (service role only).
- Parameters in msp_env_parameter (use the existing column set from migration 034; category 'hsf'): `hsf.upload_max_bytes` 26214400, `hsf.upload_allowed_mime` (comma list), `hsf.mco_transfer_mode` 'hold' (enum hold, fixture, live), `hsf.staging_alert_days` 14.
- Functions, all `security definer set search_path = public`, execute revoked from public, anon, authenticated (server side only through the service role) unless stated:
  - `hsf_consent_status(p_auth_user uuid) returns jsonb` → `{"client_account_id": uuid|null, "company_name": text|null, "wording_version": "HSF-CONSENT-1.0", "document_storage": bool, "mco_transfer": bool, "authority_to_share": bool, "complete": bool}` (true = latest row for that kind is granted and not withdrawn).
  - `hsf_record_consent(p_auth_user uuid, p_kinds text[], p_wording_version text) returns jsonb` → same shape as status. Refuses unknown kinds and a wording version other than the current one. Audits.
  - `hsf_withdraw_consent(p_auth_user uuid, p_kind text) returns jsonb` → status. Withdrawal stops new uploads; audited.
  - `hsf_register_upload(p_auth_user uuid, p jsonb) returns jsonb`. Input `{file_id?, element_code?, section_code?, department_code, original_name, mime_type, size_bytes, sha256}`. Refuses unless consent complete, the account is not declined, mime and size pass the parameters, department exists, file (if given) belongs to the account. Builds safe_name (letters, digits, dot, underscore only; max 120 chars) and storage_path. Output `{upload_id, bucket, path}`. Audits.
  - `hsf_mark_uploaded(p_auth_user uuid, p_upload_id uuid) returns jsonb` → `{upload_id, status}`; only the uploading user; awaiting_upload → uploaded.
  - `hsf_my_uploads(p_auth_user uuid, p_file_id uuid default null) returns jsonb` → array of `{upload_id, original_name, department_code, section_code, element_code, size_bytes, status, created_at, transferred_at, staging_deleted_at, mco_document_ref}`.
  - Worker side: `hsf_transfer_queue(p_limit int) returns setof hsf_upload` (status uploaded or held, oldest first); `hsf_transfer_record(p_upload_id uuid, p_mode text, p_outcome text, p_server_sha256 text, p_mco_ref text, p_receipt_sha256 text, p_error text) returns jsonb` which appends hsf_mco_transfer and moves the upload: held → status held; received with sha256_server = sha256_client = receipt → status transferred, sets mco_document_ref and transferred_at and, if a matching hsf_evidence row exists, its mco fields; hash mismatch → status failed with reason; error → stays uploaded/held. `hsf_mark_staging_deleted(p_upload_id uuid) returns jsonb` only from status transferred → staging_deleted, sets storage_path null and staging_deleted_at. The file bytes are removed by the worker through the Storage API **before** this call; the row and its hashes stay for the audit trail.
  - When an upload is registered against an element of a File, `hsf_mark_uploaded` also creates the hsf_evidence row (version n+1, source client_upload, sha256 = client hash, supplied_by = user email) and sets the file item status to 'uploaded'.

**050_kernel_api.sql.**
- `msp_instrument_currency_hold (instrument_id uuid pk fk, reason text not null, held_by text not null, held_on date not null default current_date)`. Seed holds (where the rows exist and status is still 'verified') for 'NIHL Regulations, 2003' and 'Environmental Regulations for Workplaces, 1987', reason citing their repeal from 06/09/2026 per the Noise Exposure and Physical Agents Regulations, 2024, recorded in the CNC OHS Industry Kernel (23/09/2026) and migration 042. A hold only ever tightens: it removes an instrument from citation.
- View `kernel_citable_instrument`: instruments with status 'verified', not superseded, not held. Columns: short_name, full_citation, instrument_type, gazette_reference, effective_date, verified_on, review_due, scope, industries (json array of {code,name}). Grant select to anon, authenticated.
- View `hsf_public_element_library` (SPEC B4.8): section_code, section_name, code, name, duty, universal, citable instruments (json array of short names from kernel_citable_instrument only), awaiting (json array of candidate names not yet citable). Grant select to anon, authenticated.
- `msp_api_client (id uuid pk, name text not null, owner text not null, key_prefix text not null, key_hash text unique not null, scopes text[] not null default '{kernel.read}', active boolean default true, hourly_limit int not null default 600, created_at, revoked_at)`; `msp_api_call_log (id bigint identity, client_id uuid, resource text, status int, created_at)` append only; no request bodies, no IPs.
- `msp_api_client_issue(p_name text, p_owner text, p_scopes text[] default '{kernel.read}') returns jsonb` → `{client_id, api_key}` where api_key = 'cnck_' || 32 random bytes hex, shown once; stores sha256 hex of the full key. Callable only by the service role or forge_admin. Audits (never the key).
- `msp_api_client_revoke(p_client_id uuid)` same access.
- `msp_api_authorise(p_key_hash text, p_resource text) returns jsonb` → `{ok, client_id, scopes, reason}`; checks active, not revoked, scope kernel.read, calls in the last hour below hourly_limit; logs the call. Service role only.
- Read functions returning jsonb, service role only, used by the API: `kernel_api_industries()`, `kernel_api_industry(p_code text)` (subindustries, roles, hazards, protocols with basis from citable instruments only, citable instruments), `kernel_api_instruments(p_industry text default null)`, `kernel_api_protocols(p_industry text default null)`, `kernel_api_elements(p_industry text default null)` (element library with only citable bases), `kernel_api_search(p_q text)` (simple ilike over instrument, protocol, role, element names; max 50 results). Every response object includes `"kernel_release"` (latest msp_kernel_version.semver), `"as_at"` (current_date) and `"notice"`: "Framework reference data from the Care Net Cognitive Kernel. Not legal advice and not a clinical opinion. Only instruments that have passed three verification checks and are in force are included."

**051_hsf_generate_and_compliance.sql.**
- `hsf_generate_file(p_auth_user uuid, p jsonb) returns jsonb`. Input `{industry_code, subindustry_code?, triggers: text[], scope: {sites: [{name, address?}], project_reference?, headcount?}}`. Regime MHSA when industry_code = 'MINING' else OHSA. Selects universal elements whose trigger is null or 'U' or in triggers (compound triggers 'A and B', 'A or B' as in hsf/build_samples.py), overlay elements for the industry, writes hsf_file (status 'draft', revision 1) and one hsf_file_item per element (status 'outstanding'). Returns `{file_id, reference, items}`. Requires consent complete? No: generating the skeleton holds no documents; consent is required for uploads only.
- `hsf_compute_compliance(p_file_id uuid) returns jsonb` per SPEC B9.4 (overall and per section; not_applicable leaves the denominator) and caches hsf_file.compliance_pct.
- `hsf_my_files(p_auth_user uuid) returns jsonb` → array `{file_id, reference, industry_code, industry_name, status, revision, compliance_pct, created_at}`.
- `hsf_file_detail(p_auth_user uuid, p_file_id uuid) returns jsonb` → `{file: {...}, sections: [{code, name, compliance_pct, items: [{item_id, element_code, name, duty, evidence_type, review_interval, status, reason, due_date, citable: [short names], awaiting: [candidate names], uploads: [{upload_id, original_name, status, department_code}]}]}], overall: {pct, counts}}`. Only the owning client account or staff.
- `hsf_set_item_status(p_auth_user uuid, p_item_id uuid, p_status text, p_reason text)` for not_applicable with reason (min 10 chars) or back to outstanding; audited.
- Only these functions write engagement data. All service role only (the web tier calls them after verifying the user's JWT).

## 4. Server (Vercel functions, portable Node, no npm dependencies; CommonJS `module.exports = async (req, res) => {}` like vercel/api/intake.js)

- `vercel/lib/auth.js`: `async function requireUser(req)` reads `Authorization: Bearer <access_token>`, calls `GET ${SUPABASE_URL}/auth/v1/user` with headers apikey = SUPABASE_SERVICE_ROLE_KEY and Authorization = the user token, returns `{id, email}` or throws an error with `.status = 401`. `function sendError(res, err)` maps .status.
- `vercel/api/hsf-consent.js`: GET → hsf_consent_status; POST `{kinds: [...], wording_version}` → hsf_record_consent; DELETE `?kind=` → hsf_withdraw_consent.
- `vercel/api/hsf-upload.js`: POST `{action: 'register', ...register input}` → hsf_register_upload, then `POST ${SUPABASE_URL}/storage/v1/object/upload/sign/hsf-staging/<path>` (service role) → returns `{upload_id, upload_url}` where upload_url = `${SUPABASE_URL}/storage/v1` + returned `url` (it carries the token). Browser then `PUT upload_url` with the file body and header `Content-Type`. POST `{action: 'complete', upload_id}` → hsf_mark_uploaded. GET `?file_id=` → hsf_my_uploads.
- `vercel/api/hsf-file.js`: GET → hsf_my_files; GET `?file_id=` → hsf_file_detail; POST `{action: 'generate', ...}` → hsf_generate_file; POST `{action: 'set_status', item_id, status, reason}` → hsf_set_item_status.
- `vercel/api/kernel.js`: public read API for the Grok bot and other approved clients. `GET /api/kernel?r=industries|industry|instruments|protocols|elements|search&code=&industry=&q=`. Auth header `Authorization: Bearer cnck_...`; sha256 in Node crypto; msp_api_authorise; 401 on bad key, 429 on rate limit, 400 on bad resource. JSON only; `Cache-Control: no-store`; CORS `Access-Control-Allow-Origin: *` with `Authorization` allowed (keys are server to server; no cookies).
- `server/serve.js`: dependency free Node http server that serves `vercel/` statics (cleanUrls like vercel.json) and mounts every `vercel/api/*.js` handler at `/api/<name>`, adding `req.query` and a parsed JSON `req.body` and `res.status().json()` helpers. This is the path to hosting inside MCO.
- Tests: `test/api/*.test.js` runnable with `node --test`, mocking global fetch.

## 5. MCO transfer worker

- `supabase/functions/_shared/mco-adapter.js` (plain ES module, no Deno or Node specific APIs; uses global fetch and crypto.subtle): `export async function sha256Hex(bytes)`, `export function createAdapter({mode, baseUrl, token})` returning `{ send({uploadId, fileName, mimeType, bytes, sha256, clientAccountId, departmentCode, sectionCode}) }` → `{outcome: 'held'|'received'|'error', mcoDocumentRef, receiptSha256, error}`. mode hold → held, no network. mode fixture → received with ref 'FIXTURE-' + uploadId and receiptSha256 = sha256 (for tests only). mode live → throws 'MCO interface contract pending (HSF-3)' unless baseUrl and token set; the live request shape is a documented placeholder and must not be presented as a real MCO endpoint.
- `supabase/functions/hsf-mco-transfer/index.ts` (Deno, Supabase edge function, invoked by schedule or manually with the service role): reads `hsf.mco_transfer_mode`, calls hsf_transfer_queue(10), downloads each object, computes sha256Hex, sends, calls hsf_transfer_record, and only on outcome received with matching hashes deletes the object (`DELETE /storage/v1/object/hsf-staging` with `{prefixes:[path]}`) and then calls hsf_mark_staging_deleted. Never deletes on held, error or mismatch.
- `test/mco/adapter.test.mjs` with `node --test` for hold and fixture paths and the hash check.

## 6. Front end (vercel/)

Shared files, loaded on every page in this order at the end of `<head>`: `/js/cnc-config.js`, `/js/cnc-tracking.js`; before `</body>`: `/js/cnc-design-assets.js` (defer).
- `js/cnc-config.js`: `window.CNC_CONFIG = {supabaseUrl, publishableKey, gtmId: null, whatsapp: '27600702723', mainSite, privacyUrl, authProvider: 'supabase', mcoPortalUrl: null, apiBase: ''}` with comments marking pending values.
- `js/cnc-tracking.js`: Google consent mode defaults denied (same keys and storage key `cnc_consent_v1` as medical-surveillance-plans.html so consent carries across pages); loads GTM only when gtmId is set and never before consent; if no stored choice and the page has no element with id `consent`, injects the shared consent bar (Accept / Decline, privacy link, text: "We use cookies to understand how the site is used. Tracking stays off until you accept, and your information is handled under POPIA."). Delegated click tracking: any element with `data-cta="<name>"` pushes `{event: 'cnc_cta_click', cta: name, page: location.pathname}` to dataLayer. Never pushes personal information.
- `js/cnc-auth.js` (ES module): `export const auth` with `init()`, `signInWithEmail(email, redirectTo)`, `session()`, `accessToken()`, `onChange(cb)`, `signOut()`. Provider 'supabase' uses `https://esm.sh/@supabase/supabase-js@2` exactly as index.html does. Provider 'mco_sso' is a stub that throws "MCO single sign on is pending the MCO interface contract (HSF-3)".
- `js/cnc-design-assets.js` + `css/cnc-design-assets.css`: every page carries `<script type="application/json" id="cnc-asset-spec">{"page": "...", "assets": [...]}</script>` and marks each slot with `data-asset="<id>"`. Asset object: `{id, block, purpose, type ('photo'|'illustration'|'icon'|'logo'|'pattern'|'video'|'og_image'|'document'), formats: ['webp', 'avif', 'jpg', 'svg', 'png', 'mp4', 'pdf'...], width, height, ratio, max_kb, colour, alt, file_name, delivery ('img.carenetcdn.com/...' or '/assets/...'), status ('existing'|'needed'), notes}`. When the URL has `?design=1` or `#design`, the script outlines every data-asset slot with a numbered label and opens a panel listing every asset with its full specification and a copy to clipboard (JSON). Otherwise it does nothing. No effect on Lighthouse when off.
- `hsf/build_asset_manifest.py` reads every `vercel/*.html` spec and writes `msp-forge/DESIGNER-ASSETS.md` (per page tables plus a combined checklist) and `vercel/design/assets.json`.

Pages:
- `vercel/hsf-builder.html` (the Health and Safety File builder, "Build my File" target). Shared shell (header, hero, pattern strip, footer, AutoHive stamp exactly as health-and-safety-file.html). States: (1) signed out → email sign in via cnc-auth; (2) signed in, no company account → link to `/` to register; (3) consent (three separate, unticked checkboxes, full wording, wording version shown, nothing uploads until all three are given; link to withdraw later); (4) no File yet → setup form (industry and subindustry from msp_public_industry_profile, sites, headcount, activity checklist that maps to trigger codes from the library) → generate; (5) File view: compliance header, fifteen section cards each with a drop zone ("Drop files for Section X here") and a department selector, element rows with status pills and basis (citable names, or "Awaiting verification: <candidate>"), per element drop target, upload queue showing name, size, SHA 256 computed in the browser with crypto.subtle, progress, and the lifecycle: Uploading, Uploaded, Held for MyClinicOnline, Transferred to MyClinicOnline, Removed from Care Net staging; gap report tab; set not applicable with reason. Rejects wrong types and sizes before upload with a plain message. Calls only `/api/hsf-consent`, `/api/hsf-upload`, `/api/hsf-file` with the user's access token.
- `vercel/portal.html` (the login portal that joins the commercial and medical spines). One sign in (cnc-auth). Commercial spine: company account, Medical Surveillance Plans and their status, Health and Safety Files with compliance, sign off and review status, quotations; links to /hsf-builder.html, /shop.html. Medical spine: MyClinicOnline records (certificates of fitness, training records, bookings) presented as "Arrives when MyClinicOnline is connected" with no invented data; a clearly labelled demonstration view using the sample fixture only when `?demo=1`. Staff (forge roles in app_metadata) see links to review.html and settings.html. Designed so the same page can be served from MCO: all URLs relative, all settings from cnc-config.
- Existing pages (index, pilot, method, shop, sample, industry, medical-surveillance-plans, assess, review, settings, account, health-and-safety-file, hsf-sample): add the shared scripts, a full asset spec block with data-asset slots, `data-cta` on every call to action. The main calls to action, same wording everywhere: "Build my File" → /hsf-builder.html ; "Build my Plan" → /shop.html (or / for sign in) ; "WhatsApp a sales executive" → https://wa.me/27600702723 ; "Sign in" → /portal.html. health-and-safety-file.html "Build my File" buttons point to /hsf-builder.html. Do not change page copy beyond this and do not remove existing features. medical-surveillance-plans.html keeps its own consent bar (id consent), which the shared script respects.

## 7. Legislation references on pages

A page may name an instrument as the basis for something only if it appears in `kernel_citable_instrument` (verified three ways in the kernel, in force, not held). Everything else is shown as "awaiting verification". The builder and portal read citations live; static page copy may describe duties in plain words and name the OHS Act, the Construction Regulations, 2014, COIDA, the HCA, HBA and Lead Regulations, the Noise Exposure and Physical Agents Regulations, 2024 (only once HSF-7 is closed), and the MHSA and PSIRA only as regimes, never as cited provisions. No section or regulation numbers on static pages except "section 16(2)" and "section 37(2)" of the OHS Act, which the kernel pack and the build prompt both name.

## 8. What nobody builds

No real MCO endpoint, no invented GTM ID, no invented model name for Grok (read it from an environment variable), no application to the live Supabase project, no Vercel deployment, no change to existing migrations 001 to 046.

## 9. Amendment 1 (review round, 23/09/2026): binding on the fix round

Migrations 047 to 051 are not applied anywhere, so they are corrected in place; no migration 052.

9.1 **Account linking.** `hsf_link_account(p_auth_user uuid) returns uuid` (service role only): returns the account already linked to the user; otherwise links the latest non declined `msp_client_account` whose `lower(contact_email)` equals the user's confirmed email (`auth.users.email_confirmed_at is not null`) and whose `auth_user_id` is null, audits `client_auth_linked`, and returns it. `vercel/lib/auth.js requireUser` calls it once per request after verifying the token. The test stub gains `auth.users.email_confirmed_at timestamptz` and `raw_app_meta_data jsonb`.

9.2 **Not found.** `hsf_file_detail` returns SQL null when the File does not exist or is not the caller's (no exception); `hsf_set_item_status` raises with SQLSTATE `P0002` ('no_data_found') for a missing or foreign item. The server maps null to 404 and P0002 to 404.

9.3 **File citations.** An instrument is citable **for a File element** only when it is verified, not held, not superseded, its `scope` is 'safety' or 'both', and the element's `hsf_element_instrument.provision` is not 'awaiting verification'. One SQL function `hsf_element_citable(p_element_id uuid) returns jsonb` (array of short names) is the single source used by `hsf_public_element_library`, `hsf_file_detail`, `kernel_api_elements` and the release gate. Everything else shows under `awaiting`. `kernel_citable_instrument` (the register view) keeps its current meaning. Until Phase 2 re verification, File elements therefore show "Awaiting verification", which is the truthful state.

9.4 **Currency holds everywhere.** `msp_public_instrument_register`, `msp_public_industry_profile`, `msp_public_framework_stats` and every kernel read exclude instruments in `msp_instrument_currency_hold`. Seed a hold for 'Asbestos Abatement Regulations, 2020' (reason: amendment notice number conflict GN R.2092 against GN R.11435, register HSF-9), alongside the NIHL 2003 and Environmental Regulations 1987 holds.

9.5 **Transfer queue.** Replace the worker's use of `hsf_transfer_queue` with:
- `hsf_transfer_mode() returns text` (reads `hsf.mco_transfer_mode`; service role only). The worker no longer calls msp_env_get.
- `hsf_transfer_claim(p_limit int) returns setof hsf_upload`: in mode hold returns only status 'uploaded' rows (the worker then records held); in fixture or live returns 'uploaded' and 'held' rows plus 'transferring' rows claimed more than 30 minutes ago, locks them with `for update skip locked`, sets status 'transferring', oldest first. Uploads whose account has withdrawn mco_transfer or document_storage consent are never returned.
- `hsf_transfer_record` accepts uploads in status 'transferring' as well as 'uploaded' and 'held'; outcome 'error' returns the upload to 'uploaded'.
- `hsf_transfer_cleanup_queue(p_limit int) returns jsonb` → array of `{upload_id, storage_path, reason}` for rows whose `storage_path` is not null and whose status is 'transferred' (delete pending), 'failed' or 'rejected'. The worker deletes the object and then calls `hsf_mark_staging_deleted(p_upload_id)`, which now accepts 'transferred' (→ 'staging_deleted'), 'failed' and 'rejected' (status unchanged, storage_path null, staging_deleted_at set).
- `hsf_sweep_stale_uploads(p_hours int default 24) returns int`: 'awaiting_upload' rows older than p_hours become 'failed' with reason 'The upload was not completed.'.
- `hsf_staging_alerts` view (staff only through RLS or a service role function `hsf_staging_alerts_list()`): uploads still holding bytes in staging longer than `hsf.staging_alert_days`.
- Hash mismatch revokes the evidence row created at completion, returns the File item to 'outstanding' when it has no other unrevoked evidence, sets the upload 'failed', and recomputes compliance.
- `hsf_mark_uploaded` locks the File item row (`for update`) before choosing the next evidence version, and recomputes compliance.
- Consent withdrawal of mco_transfer or document_storage: untransferred uploads of that account get `transfer_blocked_reason = 'consent withdrawn'` (new nullable column) and are excluded from claims; nothing is deleted automatically (Director and Information Officer decision, register item).

9.6 **Rate limits.** `hsf_generate_file` refuses more than `hsf.files_per_account_per_day` (parameter, default 20) Files per account per day; `hsf_next_reference` never truncates (`\d{3,}` and `lpad(n, greatest(3, length(n)))`).

9.7 **Builder support views** (anon select): `hsf_public_trigger (code, description)` and `hsf_public_subindustry (code, name, industry_code, selectable)`.

9.8 **Portal summary.** `hsf_portal_summary(p_auth_user uuid) returns jsonb` (service role only, read only, no tokens minted): `{account: {client_account_id, company_name, account_kind, approved_at} | null, plans: [{engagement_id, reference, status, industry_code, revision, created_at}], quotes: [{quote_reference, package_code, price_zar, price_status, valid_until, created_at}], files: [{file_id, reference, industry_code, status, revision, compliance_pct, signoffs: [{kind, decision, decided_at}]}]}`. Plans come from engagements whose intake consumed an access token of the account (`msp_form_access.client_account_id` and `used_by_intake`); quotes by the account's contact email. Endpoint `GET /api/portal-summary` (vercel/api/portal-summary.js) with the same requireUser. portal.html uses it instead of /api/company-lookup.

## 10. Amendment 2 (Director's decisions, 23/09/2026): binding on the launch controls round

All database changes in this round go in one new migration, `supabase/migrations/052_hsf_launch_controls.sql`, which must replay cleanly after 051 with `test/sql/replay.sh`. Migrations 047 to 051 are not edited in this round except where 10.9 says so (048 via its generator). Checks for 052 go in a new file `test/sql/hsf_launch_checks.sql` (same style as hsf_flow_checks.sql: one transaction, rolled back, `raise notice 'ok   ...'` per check, ends with a notice that all passed).

10.1 **Uploads stay closed until the Director opens them.** New parameter `hsf.uploads_open` (boolean, default `false`, group hsf, forge_admin sets it through `msp_env_set`). Description: "Opens company uploads in the File builder. Stays off until the backend (Odendaal) and the front end (Cassandra and the designer) are signed off." `hsf_register_upload` refuses while it is false with exactly: 'Document uploads open soon. Your File can be built now, and uploads will open once Care Net has finished testing.' Consent, File generation and item status still work while closed. New service role function `hsf_upload_gate(p_auth_user uuid) returns jsonb` → `{uploads_open bool, client_verified bool, verification_requested bool, consent_complete bool}` for the builder.

10.2 **Only verified Care Net clients upload.** `account_kind = 'approved_client'` is not enough (migration 040 self approves). New table `hsf_client_verification (client_account_id uuid pk references msp_client_account, status text check in ('requested','verified','revoked'), method text check in ('mco_company_ref','client_register','sales_executive') null until verified, evidence_ref text, requested_at, verified_by text, verified_at, revoked_by text, revoked_at, revoke_reason text)`, RLS on, staff read only, service role all. Functions:
- `hsf_request_client_verification(p_auth_user uuid) returns jsonb` (service role): creates or returns the request for the caller's account; audits `hsf_client_verification_requested`.
- `hsf_verify_client(p_client_account_id uuid, p_method text, p_evidence_ref text) returns jsonb` (authenticated, forge_admin or `hsf_is_staff()` only, checked inside; also service role): sets verified, records `verified_by` from the JWT email or 'service_role', requires a non empty evidence_ref (for example the MCO company reference or the client register number); audits.
- `hsf_revoke_client_verification(p_client_account_id uuid, p_reason text)` (same callers): revokes; the account's untransferred uploads are NOT deleted, they get `transfer_blocked_reason = 'client verification revoked'`.
- `hsf_client_verified(p_client_account_id uuid) returns boolean`.
`hsf_register_upload` refuses an unverified account with: 'Uploads are for verified Care Net Consultants clients. Ask for verification in the builder, or WhatsApp a sales executive.' Order of refusals in hsf_register_upload: no account, declined, uploads closed (10.1), not verified, consents, then the existing checks.

10.3 **Longest stay in staging: two years.** New parameter `hsf.staging_retention_days` (integer, default 730, min 1, max 730). New status `expired` on hsf_upload (replace the status check constraint to add `expired` and `client_deleted`, keeping every existing value). `hsf_retention_queue(p_limit int) returns jsonb` (service role) → `[{upload_id, storage_path, reason: 'retention'}]` for rows holding bytes (`storage_path is not null`) whose `coalesce(uploaded_at, created_at)` is older than the parameter, INCLUDING blocked uploads (the two year limit overrides a block). The worker deletes the object and calls `hsf_mark_expired(p_upload_id uuid)` which sets status `expired`, clears storage_path, sets staging_deleted_at, revokes the evidence row as in the hash mismatch path (item back to outstanding when nothing else holds it; recompute compliance), audits `hsf_upload_expired`. `hsf_staging_alerts` and `hsf_staging_alerts_list()` gain a column `expires_on date` and also list rows within 30 days of expiry. The builder shows "Kept in Care Net staging until <date>" for staged uploads (hsf_my_uploads gains `expires_on`).

10.4 **Every upload is scanned before it can move.** New columns on hsf_upload: `scan_status text not null default 'pending' check in ('pending','clean','infected','harmful','error')`, `scan_engine text`, `scanned_at timestamptz`, `scan_findings jsonb`. Rules:
- `hsf_transfer_claim` returns only rows with `scan_status = 'clean'`. In hold mode a clean upload still becomes held as today.
- `hsf_scan_claim(p_limit int) returns setof hsf_upload` (service role): uploads in status 'uploaded' with scan_status 'pending' or 'error' (error retried, at most 5 attempts tracked in a new column `scan_attempts int not null default 0`), not blocked, bytes present, `for update skip locked`, oldest first; increments scan_attempts.
- `hsf_scan_record(p_upload_id uuid, p_result text, p_engine text, p_findings jsonb) returns jsonb` (service role): p_result in ('clean','infected','harmful','error'). infected or harmful → status 'rejected', reject_reason 'The file failed the security scan: <plain summary from findings>', evidence revoked as in the hash mismatch path, audit `hsf_upload_scan_rejected`; the bytes then leave through the existing cleanup queue (rejected rows are already listed there). clean → scan_status clean. error → scan_status error, stays uploaded.
- Worker: new module `supabase/functions/_shared/scan-core.js` (plain ES module, no dependencies, global fetch and crypto.subtle only) exporting `inspectBytes({bytes, mimeType, fileName}) → {verdict: 'clean'|'harmful', findings: [{code, message}]}` and `createAvEngine({endpoint, token, fetch})` and `scanUpload(row, deps)`. inspectBytes, the built in structural check, always runs and flags as harmful: content whose magic bytes do not match the declared type (PDF `%PDF-`, PNG, JPEG, OOXML zip `PK\x03\x04`, legacy Office OLE `D0 CF 11 E0`, CSV must be valid UTF 8 text without NUL bytes); PDF containing `/JavaScript`, `/JS`, `/Launch`, `/OpenAction` with JavaScript, `/EmbeddedFile`, `/RichMedia`, `/XFA`, or `/AA`; OOXML containing `vbaProject.bin`, `activeX`, `oleObject` embeddings or relationships with `TargetMode="External"` (read the zip central directory and the `_rels/*.rels` and `word/_rels`/`xl/_rels` entries; stored and deflated entries, deflate via `DecompressionStream('deflate-raw')` where available, otherwise flag 'could not inspect' as error, not clean); legacy Office files containing a `Macros` or `_VBA_PROJECT` stream name or the bytes `VBA`/`Attribute VB_` (flag as harmful: macros); CSV cells beginning with `=`, `+`, `-`, `@`, tab or carriage return followed by formula like content (formula injection); JPEG or PNG carrying EXIF GPS tags (location metadata) or oversized metadata segments over 256 KB. Findings carry plain words for the client. `createAvEngine`: HTTP antivirus engine at `HSF_AV_ENDPOINT` (env; for example a ClamAV REST service hosted by Care Net or MCO), bearer `HSF_AV_TOKEN`; request `POST <endpoint>` with the raw bytes, `Content-Type: application/octet-stream`, header `X-File-Name`; expected reply `{clean: boolean, signature: string|null, engine: string}`. The engine is mandatory: with no endpoint configured the scan records `error` with finding 'Antivirus engine not configured' and the file never transfers. Only the test environment variable `HSF_SCAN_ALLOW_FIXTURE=1` permits a fixture engine that answers clean (never set in production). Order in `runTransfer`: mode, sweep, scan pass (`hsf_scan_claim(10)` → download → sha256 must equal sha256_server else record error → inspectBytes → AV engine → hsf_scan_record), then transfer claim, then cleanup, then retention. One document in memory at a time.
- The builder shows the scan state: 'Security scan pending', 'Passed security scan', 'Failed security scan: <reason>'.

10.5 **Deletion by the client, with warning and PIN.** A client may permanently delete their own upload while its bytes are still in Care Net staging (any status except transferred, staging_deleted, expired, client_deleted). Once MyClinicOnline holds a document, deletion is MCO's process and the builder says so. Journey: select uploads, read the warning 'Deleting is permanent. Care Net cannot recover a deleted document, and any File item it supported will return to outstanding.', tick 'I understand this cannot be undone', choose email or SMS, receive a one time PIN, enter it, confirm. The PIN is sent and checked by Supabase Auth (no PIN is generated or stored by Care Net): the server calls `POST ${SUPABASE_URL}/auth/v1/otp` with `{email}` (or `{phone}` for SMS) and `create_user: false`, and checks with `POST ${SUPABASE_URL}/auth/v1/verify` `{type: 'email'|'sms', email|phone, token}`; a 200 with a user whose id equals the signed in user is a pass. SMS is offered only when the auth user has a confirmed phone and parameter `hsf.deletion_sms_enabled` (boolean, default false; needs an SMS provider configured in Supabase Auth) is true. Table `hsf_deletion_request (id uuid pk, client_account_id, auth_user_id, upload_ids uuid[] not null, channel text check in ('email','sms'), acknowledged_irreversible boolean not null check true, status text check in ('pending','confirmed','expired','cancelled','locked'), attempts int default 0, requested_at, expires_at (requested_at + 10 minutes), confirmed_at)`. Functions (service role): `hsf_deletion_request_create(p_auth_user uuid, p_upload_ids uuid[], p_channel text, p_acknowledged boolean) returns jsonb {request_id, expires_at, channel, destination_hint}` (every upload must belong to the caller's account and be deletable; at most 50; at most 5 open requests per account per hour); `hsf_deletion_request_attempt(p_auth_user uuid, p_request_id uuid) returns jsonb {allowed, attempts_left}` (increments attempts before the server calls verify; 5 attempts then 'locked'; expired after 10 minutes); `hsf_deletion_request_confirm(p_auth_user uuid, p_request_id uuid) returns jsonb` (only after the server verified the PIN; pending and not expired only; sets each upload `client_deleted`, `transfer_blocked_reason = null`, revokes evidence as in the mismatch path, recomputes compliance, audits `hsf_upload_client_deleted` with the fingerprints but never the bytes). The cleanup queue lists `client_deleted` rows holding bytes (reason 'client_deleted'), INCLUDING ones that were blocked; `hsf_mark_staging_deleted` accepts `client_deleted` and `expired` (status kept, storage_path null). Server: `vercel/api/hsf-delete.js`: POST `{action: 'request', upload_ids, channel, acknowledged}` → create, then send the PIN; POST `{action: 'confirm', request_id, pin}` → attempt, verify with Supabase Auth, confirm; answers never reveal whether a PIN was close. PIN must be 6 to 10 digits. Rate limit refusals are 429.

10.6 **Consent withdrawal.** Unchanged behaviour (blocked, nothing deleted automatically) plus: the builder shows blocked uploads with their `transfer_blocked_reason` and offers the 10.5 deletion journey for them, and the two year limit (10.3) still applies.

10.7 **MCO builds the transfer protocol.** Until MCO delivers it, files stay in Supabase staging (hold mode). Write `msp-forge/hsf/MCO-TRANSFER-REQUIREMENTS.md`: what Care Net's worker sends and expects (the adapter's send contract, receipt with SHA 256, idempotency on upload_id, person and company matching, error codes, deletion at MCO, audit, security: mutual TLS or signed requests, POPIA special personal information handling, retention), written as requirements for the MCO team, clearly marked as Care Net's proposal awaiting MCO's design.

10.8 **File sign off.** Research and rule: hsf/SIGNOFF-CRITERIA.md. Built after this round as migration `053_hsf_signoff_rule.sql`, not by the launch controls builders: the OMP is no longer a File signatory (the OMP signed plan is Section E evidence); release needs an approved `safety_content` sign off whose registering body and category fit the File type and whose registration is current and register checked, plus the client `client_16_2_acceptance`; the chief executive acknowledgement is recorded, not a gate. The safety content signatory must also have an appointment letter and an engagement letter on record (document reference, date, and a nullable `recruitment_portal_ref` for each, so the recruitment portal Care Net is building can supply them later); a missing letter refuses the release.

10.9 **Legislation clean up.** (a) Delete `vercel/downloads/CNC-Legislation-Register-v1.0.0.pdf` and `.csv`; move the CSV to `msp-forge/hsf/sources/CNC-Legislation-Register-v1.0.0.csv` and point `hsf/build_seed.py` at it; add a `vercel.json` redirect from the two old URLs to `/shop.html#law` (check vercel.json syntax in the repo) and the same in `server/serve.js` if it handles redirects. (b) `form/fields.json` and `form/assess.html`: the noise item names the Noise Exposure Regulations, 2024 (replacing the NIHL Regulations, 2003 from 6 September 2026) and the asbestos item reads 'Work with asbestos', matching vercel/assess.html. (c) Element names in `048_hsf_library_seed.sql` (generated by `hsf/build_seed.py` from SPEC B6) carry no section, regulation or annexure numbers other than 16(2) and 37(2): the generator rewrites them in plain words (the same mapping as hsf/build_samples.py) and fails if a number survives; regenerate 048; the provision numbers stay out until Phase 2 pins them.

10.10 **Grok always on the latest model.** `grok/bridge-example.mjs` supports `XAI_MODEL=auto` (and treats an unset XAI_MODEL as auto): on start and then at most once a day it calls xAI's OpenAI compatible `GET https://api.x.ai/v1/models` with the xAI key, keeps ids that start with `grok`, drops ids containing `image`, `imagine`, `vision`, `embed`, `mini`, `fast` or `code`, and picks the one with the greatest `created`; if the call fails it keeps the last good choice, and it logs the chosen id. No model name is written in any file. KERNEL-API.md and the security checklist say this, and that Odendaal should confirm against xAI's own documentation whether an official latest alias exists and prefer it.

10.11 **Documents to update** after the build: HSF-PORTAL-ARCHITECTURE.md (lifecycle, open items), SPEC.md register (HSF-1, HSF-19, HSF-20, HSF-22 approved), KERNEL-API.md, BUILD-STATE.md, backup (rebuild script and MANIFEST with 052), DESIGNER-ASSETS.md if page specs change.

## 11. Amendment 3 (Director's answers of 23/09/2026 on the launch controls): binding on the console and metadata round

All database changes go in one new migration `supabase/migrations/054_hsf_console_and_metadata.sql` (052 and 053 are not edited), with checks in a new `test/sql/hsf_console_checks.sql` in the style of hsf_launch_checks.sql. Every existing check file must still pass after a full replay 001 to 054 (update an existing check only where 054 legitimately changes behaviour, keeping its intent).

11.1 **All metadata removed before a file leaves staging (content disarm).** The Director's rule: scan for viruses, malware and all metadata. The scan pass, after the structural check and the antivirus engine pass, produces a cleaned copy with all non essential metadata removed and writes it back over the staged object at the same path (the original bytes are not kept). Module `supabase/functions/_shared/metadata-clean.js` (plain ES module, no dependencies, Deno and Node 22) exports `cleanMetadata({bytes, mimeType, fileName}) → {outcome: 'cleaned'|'unchanged'|'refused', bytes, removed: [{code, message}], reason}`:
- JPEG: drop every APPn segment except APP0 JFIF and APP2 ICC profile (so EXIF, XMP, IPTC and thumbnails go) and every COM segment.
- PNG: drop tEXt, zTXt, iTXt, eXIf, tIME and any private ancillary chunk; keep IHDR, PLTE, IDAT, IEND, tRNS, gAMA, cHRM, sRGB, iCCP, pHYs, sBIT, bKGD. Recompute nothing else (chunk CRCs of kept chunks are unchanged).
- OOXML (docx, xlsx): rebuild the zip without `docProps/custom.xml`, `docProps/thumbnail.*`, comments parts (`word/comments*.xml`, `xl/comments*.xml`, `xl/threadedComments/*`, `word/people.xml`, `xl/persons/*`) and their relationships and content type overrides; replace `docProps/core.xml` with a minimal core properties part (no creator, lastModifiedBy, title, subject, keywords, description, category, revision; created and modified set to the upload date) and strip `Company`, `Manager`, `HyperlinkBase` and `Template` from `docProps/app.xml`; remove `w:rsid*` attributes is NOT required. Write stored or deflated entries (CompressionStream('deflate-raw')) with correct CRC 32 and sizes; the result must reopen as a valid zip whose central directory matches its local headers.
- PDF: blank every value in the trailer `/Info` dictionary and every XMP metadata stream in place with spaces of the same byte length, so every cross reference offset stays valid; if the Info dictionary or a metadata stream sits inside a compressed object stream, or the file uses encryption, refuse with 'This PDF keeps its details in a form Care Net cannot clean. Please print it to a new PDF or save it again, then upload it.'.
- Legacy Office (doc, xls): refuse with 'Older Word and Excel files cannot have their hidden details removed. Please save it as a .docx or .xlsx file and upload it again.'.
- CSV: unchanged (no metadata), but a UTF 8 byte order mark is removed.
- Anything the cleaner cannot parse is refused, never passed as is.
The scan pass then: refused → hsf_scan_record with result 'harmful' and the refusal reason as a finding (the upload is rejected; the client reads the plain reason); cleaned or unchanged → upload the cleaned bytes over the staged object (Storage upload with `x-upsert: true`), compute its SHA 256, then call the new `hsf_scan_record_clean(p_upload_id, p_engine, p_findings, p_sha256_clean, p_metadata_removed jsonb, p_size_bytes bigint)` which records scan clean together with the cleaned fingerprint. New hsf_upload columns: `sha256_clean text check (~ '^[0-9a-f]{64}$')`, `metadata_removed jsonb`, `cleaned_at timestamptz`, `size_bytes_clean bigint`. `hsf_transfer_claim` requires `sha256_clean is not null` as well as scan clean. `hsf_transfer_record` compares the server and receipt fingerprints with `sha256_clean` (the client fingerprint stays on record as the proof of what the client sent); the worker's delete rule becomes server = receipt = sha256_clean. The plain `hsf_scan_record(..., 'clean', ...)` path is refused from 054 on (a clean result must carry the cleaned fingerprint). hsf_my_uploads returns `metadata_removed` (list of plain labels) so the builder can say 'Hidden details removed: author, company, location'.

11.2 **A file on its way to MyClinicOnline can never be deleted by the client.** `hsf_upload_deletable` refuses status 'transferring' at any age (no 30 minute window). If the transfer errors, the upload returns to 'uploaded' and becomes deletable again. The late receipt backstop stays.

11.3 **PIN attempts.** At most 5 PIN attempts per account per hour (052 allows 10). Per request stays 5.

11.4 **Sign off credentials checked at the point of use (best practice).** New parameter `hsf.signoff_register_check_max_days` (integer, default 30, min 1, max 365): the release gate refuses a safety content sign off whose register check is older than that many days before the decision day, as well as one dated after it (053). Letters must be dated on or before the decision day (053 already). New view `hsf_signatory_expiry_alerts` (staff read) and service role function `hsf_signatory_expiry_alerts_list()`: signatories whose registration expires within 60 days or has expired, with the Files they signed that are not yet released.

11.5 **Staff console.** New page `vercel/hsf-staff.html` (shared shell, the same scripts, data-cta on every action, an asset spec block like the other pages) and API `vercel/api/hsf-staff.js`. The API uses requireUser and then `hsf_user_is_staff(p_auth_user)`; anyone else gets 403. Every write goes through new service role functions in 054 that take `p_auth_user`, re check staff in the body, and record the staff member's email in `verified_by`, `recorded_by` or the audit row:
- Client verification: `hsf_staff_verification_list(p_auth_user)` (requested, verified and revoked accounts with company, contact email, requested_at, method, evidence_ref); `hsf_staff_verify_client(p_auth_user, p_client_account_id, p_method, p_evidence_ref)`; `hsf_staff_revoke_client(p_auth_user, p_client_account_id, p_reason)`.
- Scans and staging: `hsf_staff_scan_list(p_auth_user)` (uploads with scan pending or error, attempts, last findings, scan exhausted flag, staging alerts with expires_on); `hsf_staff_scan_reset(p_auth_user, p_upload_id)` (wraps hsf_scan_reset).
- Signatories: `hsf_staff_signatory_list(p_auth_user)`; `hsf_staff_signatory_save(p_auth_user, p jsonb)` creating or correcting a hsf_signatory row with every 053 field (body, category, registration number and expiry, register checked on and proof reference, appointment letter reference, date and recruitment_portal_ref, engagement letter the same, scope); the 053 freeze after release still applies.
- Sign offs: `hsf_staff_signoff_record(p_auth_user, p jsonb)` for kinds safety_content (with signatory_id and scope), client_16_2_acceptance and ceo_16_1_acknowledgement (signatory name and the reference of the signed acceptance document), decision approved, amended or rejected, decided_at; refuses omp_medical.
- Release readiness: `hsf_release_readiness(p_file_id uuid, p_revision int) returns jsonb {ready bool, missing: [plain reasons]}` that runs the same rules as hsf_release_gate without inserting anything (refactor so the gate and the readiness check share one function), and `hsf_staff_file_list(p_auth_user)` (Files with company, industry, revision, compliance, sign off state and readiness).
- Signatory expiry alerts (11.4).
The page: four tabs (Clients to verify, Scans and staging, Signatories, Sign offs and readiness); forms with labels, validation, plain errors; confirmation built into the page for revocation and scan reset (no alert or confirm dialogs); keyboard accessible; works at 390 pixels. Staff role names never appear to clients. Demo mode `?demo=1` shows realistic sample rows clearly marked as samples.

11.6 **Grok setup script for Odendaal.** `grok/setup.mjs` (Node 20 or later, no dependencies), run as `node grok/setup.mjs` on the bot host: reads `XAI_API_KEY`, `CNC_KERNEL_API_KEY` and `CNC_KERNEL_API_BASE` from the environment or an existing `.env` next to it (never prints a key, never writes one anywhere else); checks the Node version; calls the kernel API (`GET <base>/api/kernel?r=industries`) and reports success or the exact status; calls xAI `GET https://api.x.ai/v1/language-models` (falling back to `/v1/models`) and chooses the model: an official alias that ends in `latest` for the flagship Grok family if xAI lists aliases, otherwise the same newest Grok rule as the bridge (move that rule into `grok/model-pick.mjs`, used by both); writes or updates `XAI_MODEL=<chosen id>` in the `.env` (or leaves `XAI_MODEL=auto` with `--auto`); `--check` runs without writing. Clear numbered output and exit codes. A test with mocked fetch. Document it in KERNEL-API.md (how Odendaal runs it) and add `.env` to a `grok/.gitignore`.

11.7 **Retention review.** The two year limit stays until the Director has audited what the report builder can do; record this in SPEC HSF-19.

## 12. Amendment 4 (Director's instruction, 23/09/2026): guidance for a first File

Many companies will be building their first Health and Safety File. Every section, every File item, every appointment type and every training or medical class explains, in plain words: what to submit, why it matters, and an example.

12.1 **Source of truth.** `hsf/guidance/` holds the guidance as JSON, one fragment per writer (`fragment-*.json`), merged by `hsf/build_guidance.py` into `hsf/guidance/guidance.json`. The worklist of every code is `hsf/guidance/worklist.json` (sections 15, elements 256, appointments 41, classes 34). Shape:
- `sections.<code>`: `{intro, what_goes_here: [..], who_usually_holds: [department codes], first_file_tips: [..]}`
- `elements.<code>`, `appointments.<code>`, `classes.<code>`: `{what_to_submit: [2 to 5 short items], why: 'one to three sentences', example: 'a concrete fictitious example', common_gaps: [0 to 3 items], suggested_department: '<department code>'}` (suggested_department only for elements).
- `first_file`: `{intro, order: [{section, reason}], before_you_start: [..]}`: the recommended order to tackle the sections on a first File.
- `meta`: `{version: 'HSF-GUIDE-1.0', status: 'draft', reviewed_by: null, note}`.

12.2 **Writing rules.** South African British English, plain words for a small business owner, second person ("your"). No section, regulation, annexure or Gazette numbers except OHS Act section 16(2) and section 37(2). Instruments may be named only as the contract section 7 allows (the OHS Act, the Construction Regulations, 2014, COIDA, the Hazardous Chemical Agents, Hazardous Biological Agents and Lead Regulations; the MHSA and PSIRA only as regimes; never the NIHL Regulations, 2003 or the Environmental Regulations for Workplaces, 1987 as current). Never invent a legal interval, threshold, fee or deadline; use only the element's own review_interval and describe it in words; otherwise say "as your risk assessment or the practitioner sets". Examples use fictitious South African companies, people and places and are marked as examples; never real people or real registration numbers. Never "compliant", never "guarantees", no dash or hyphen punctuation in prose, rand as R4 250,00, staff are sales executives, Care Net screens fitness and does not diagnose. Medical items say that clinical records stay with the occupational health practitioner and only the outcome (fit, fit with restrictions, unfit) goes in the File.

12.3 **Database.** Migration `055_hsf_guidance.sql`, generated by `hsf/build_guidance.py` from guidance.json (never edited by hand; the generator refuses unknown codes and missing codes): tables `hsf_section_guidance`, `hsf_element_guidance`, `hsf_appointment_guidance`, `hsf_class_guidance` and `hsf_guidance_meta`, RLS on, readable by anon and authenticated (reference content, nothing personal), written only by the service role; view `hsf_public_guidance` returning one jsonb document like guidance.json. Checks in `test/sql/hsf_guidance_checks.sql`: every element, appointment, class and section has guidance; no number patterns other than 16(2) and 37(2); counts match the worklist. **Frozen (24/09/2026):** 055 is applied to the live project, so `hsf/build_guidance.py` no longer writes it: it pins 055's SHA-256, writes only guidance.json and guidance.js, and every guidance change after 055 is carried by a new migration from `python3 hsf/build_guidance.py --delta` (the first is 056); `--check` fails until it is. The seed generator `hsf/build_seed.py` is frozen the same way for 048 (section 3).

12.4 **Front end.** The generator also writes `vercel/hsf/guidance.js` (`window.CNC_HSF_GUIDANCE = {...}`), loaded by the builder in both live and demonstration modes. In `vercel/hsf-builder.html`:
- a "First File? Start here" panel above the section cards: the intro, what to have ready, and the recommended order, each step linking to its section;
- on every section card, a "What goes in this section" disclosure (intro, typical documents, who usually holds them, tips), and the department selector preselects the first of who_usually_holds;
- on every element row, a "What to submit, why, and an example" disclosure with the four parts; a per appointment or per class item shows its appointment or class guidance;
- disclosures are native details elements, keyboard accessible, closed by default, and work at 390 pixels;
- a small note that the guidance is general, is being reviewed by Care Net's Health and Safety Manager, and does not replace the practitioner's advice.
`vercel/hsf-sample.html` sample items show the same disclosure (build_samples.py reads guidance.json).

12.5 **Call to action: onboard a registered Health and Safety practitioner through the Care Net Recruitment Portal** (Director's instruction, 23/09/2026). `vercel/js/cnc-config.js` gains `recruitmentPortalUrl: null` (pending: the address is not known; never invent it). One shared call to action, wording "Onboard a registered Health and Safety practitioner", `data-cta="recruitment_onboard_hs_practitioner"`, with a one line explanation: "Need a SACPCMP or SAIOSH registered practitioner to sign your File, or a competent person for an appointment? Care Net's Recruitment Portal places and onboards them, with the appointment and engagement letters your File needs." When recruitmentPortalUrl is null the button goes to WhatsApp a sales executive (`https://wa.me/27600702723`) and says "Ask a sales executive about onboarding a registered Health and Safety practitioner"; when set, it opens the portal with `?source=hsf&need=<signatory|appointment>&industry=<code>` where known. Placement: in the builder on the File header (sign off readiness), on Section B (appointments) and on every appointment item and the safety content sign off item whose status is outstanding; on health-and-safety-file.html in the sign off section; on hsf-sample.html; in the staff console Signatories tab ("Find a practitioner in the Recruitment Portal"). The appointment and engagement letter fields in the staff console accept the recruitment portal reference (053) and say so. Every placement has a data-asset free layout (no new image slots).

12.6 **Call to action on Section N: onboard a registered, competent auditor** (Director's instruction, 23/09/2026). Section N (Audit, review and improvement) carries a second recruitment call to action, "Onboard a registered, competent auditor", `data-cta="recruitment_onboard_auditor"`, with the explanation: "Your File should be audited by someone whose registration and experience fit your industry. Care Net's Recruitment Portal matches an auditor to your industry and onboards them, with the appointment and engagement letters on record." It appears on the Section N card and on every outstanding Section N audit item, on the sample File's Section N, and in the staff console Signatories tab. Industry fit is described, never invented: the builder shows a short credentials line per regime from one table in the page script (construction: SACPCMP registration in a construction health and safety category; mining: experience under the Mine Health and Safety Act regime; every other industry: a registered health and safety practitioner, SACPCMP or SAIOSH, and, where the company runs a certified management system, an ISO 45001 lead auditor qualification), marked as Care Net's standard for the Recruitment Portal to confirm. Fallback and link as 12.5, with `need=auditor&industry=<code>`.

12.7 **Every legislation reference links to an example on Care Net letterhead and to a legislation background** (Director's instruction, 23/09/2026).
- Examples: for every section, at least one fictitious, clearly marked example of its key document (for example Section B: a signed section 16(2) assignment letter; Section A: a legal register extract; Section N: an internal audit report), written as JSON in `hsf/examples/` and generated by `hsf/build_examples.mjs` to `vercel/examples/<section>-<slug>.docx` and `.pdf` on the External CNC letterhead exactly as the CNC Letterhead Standard v1.0.0 sets (header first page only, footer every page, Arial, 1.5 spacing, justified, exact margins and image transforms, both DOCX and PDF). The letterhead source `CNC_Letterhead_-_External.docx` is not in the repository or the build environment; generation waits until the Director supplies it, and nothing is drawn to imitate it. Client facing examples follow the standard's IP rules (no internal platform names).
- Legislation background: new page `vercel/legislation.html` with one anchor per instrument (`#<instrument code or slug>`), reading the live register views (`msp_public_instrument_register` and the currency holds) for the citation, Gazette reference, verification date and next review, and showing the kernel's status honestly: verified through the kernel's three checks, or "Awaiting verification", or "Held" with the reason; never presenting an unverified instrument as triple verified. A plain words background per instrument (what it covers, who it applies to, what a company typically keeps as evidence) from `hsf/legislation/backgrounds.json`, with the same writing rules as 12.2.
- Links: every instrument named on the section overview grid, section cards, element disclosures and the sample File links to its background anchor; every section links to its letterhead example(s) once generated (hidden until the files exist).

12.8 **A Health and Safety File is never presented as a Medical Surveillance Plan** (Director's instruction, 23/09/2026: "your sample plans cannot be the same plan as a medical surveillance plan; this is something completely different").
- The File pages (health-and-safety-file.html, hsf-builder.html, hsf-sample.html, hsf-staff.html) get their own site menu: "Health and Safety File" (/health-and-safety-file.html), "Build my File" (/hsf-builder.html), "Sample File" (/hsf-sample.html), "Sign in" (/portal.html). They no longer carry the Medical Surveillance menu items ("Build your Plan", "Sample Plan") or the "Sample Medical Surveillance Plan" chip. Where a File page mentions the Medical Surveillance Plan at all (Section E evidence), it says it is a separate Care Net product signed by the OMP.
- The portal, which joins both, labels the two samples separately and unambiguously: "Sample Medical Surveillance Plan" and "Sample Health and Safety File".
- The sample File has its own document identity: title "Sample Health and Safety File", the fifteen sections, the compliance matrix and gap report; never the Medical Surveillance Plan cover, headings, protocol tables or the words "Sample Plan". Any future sample File PDF gets its own cover and generator, never factory/render_sample_pdf.py's Plan cover.
- A check (test/api or a small node test) scans the File pages for "Sample Plan", "Build your Plan" and "Sample Medical Surveillance Plan" and fails if any remains outside a sentence that names the Plan as a separate product.

## 13. Amendment 5 (Director's instruction, 24/09/2026): appointments, the Be-Matched portal, engagement monitoring and the trust account

13.1 **Two routes for every appointment.** Every appointment item (the 41 appointment types) can be filled internally (a company employee is appointed) or outsourced through the Care Net Consultants **Be-Matched** recruitment portal, which matches a qualified person. The recruitment calls to action of 12.5 and 12.6 point to Be-Matched (address still pending; WhatsApp a sales executive meanwhile).

13.2 **Market information per appointment type** (`hsf/appointments/market.json`, from desk research with sources, dated, marked indicative): how the industry mostly engages the role (permanent, fixed term contract, per site or per project, part time or on call, or usually a duty added to an existing employee's role), the low and high end of market pricing in rand with the unit (per hour, per day, per month, per site visit, or annual cost to company), the qualifications and accreditations that are critical for the role, and the registering body where one exists. Prices are never invented: every figure carries its source and date or the item says "no published market price found".

13.3 **Three documents per appointment.** Every appointment needs, on file: (a) the **appointment letter**, (b) the **scope of work**, and (c) the **engagement letter** (for an outsourced appointment, the engagement between the company, the person and Care Net; for an internal appointment, the internal acceptance). Each is an evidence slot on the appointment item, with the recruitment portal reference where Be-Matched supplied it. Qualifications and accreditations are evidence too: a certificate or register check per critical credential. An appointment is complete only when all three documents and the critical credentials are on file.

13.4 **Engagement monitoring by Grok bots (human decides).** Engagement letters and their scope of work are monitored for discrepancies (scope versus what was done, missed inspections or registers, expired credentials, late or missing reports). The bot raises a flag with its evidence to a named Care Net person and to the company; it never decides, terminates, withholds payment or penalises anyone on its own (POPIA's rule on decisions based solely on automated processing). Its access is limited to the engagement records it monitors, logged, and separate from the public kernel API keys. Design first (`hsf/appointments/ENGAGEMENT-MONITORING.md`); no live bot access until the Director approves the design and the operator agreement with xAI.

13.5 **Payments held in trust.** Money for each engagement is held for the parties in a trust or escrow arrangement and released against milestones. Because holding other people's money is regulated, the structure (attorney's trust account, registered escrow provider, or other) and its compliance duties are decided by the Director with the attorney before anything is built. The build records the design only (`hsf/appointments/TRUST-ACCOUNT-DESIGN.md`): parties, milestones, release rules, dispute path, reconciliation, audit and POPIA; no payment code, no bank integration, no stored account numbers.

13.6 **Placement law.** Before Be-Matched places people, the Director confirms with the attorney whether Care Net must register as a private employment agency and how the rules on temporary employment services apply to placed workers; recorded as a register item.

## 14. Amendment 6 (Director's answers, 24/09/2026)

14.1 **Deletion in transit and at MyClinicOnline.** Confirmed: a document on its way to MyClinicOnline, or held there, is never deleted on request. It is deleted only when its retention period under the applicable legislation expires (for medical records, the retention the kernel's retention rules set). The client deletion journey (10.5) applies only while a document is still in Care Net staging and not in transit. MCO's transfer protocol (hsf/MCO-TRANSFER-REQUIREMENTS.md) must hold each document for its legislated retention and delete it on expiry, with an audit record.

14.2 **Letterhead.** The Director supplied CNC_Letterhead_-_External.pdf. Its two images are the standard's cropped header (2000 x 304) and footer (2000 x 427) at the standard's positions; they are held in hsf/examples/letterhead/ and used by hsf/build_examples.mjs (12.7).

14.3 **Recruitment portal name and address.** The portal is **Bee-Matched** (Care Net Consultants). The future address given, "www.carenetconsultants/recruitment_Bee_Matched.co.za", is not a valid web address as written; recruitmentPortalUrl stays null (WhatsApp fallback) until the exact address is confirmed and live. Visible text says "Care Net's Bee-Matched recruitment portal".

14.4 **Legal decisions** on Bee-Matched placements and the trust account are with the Director and the attorney (hsf/appointments/LEGAL-STRUCTURE-RESEARCH.md); nothing that places people or holds money is built until they decide.

14.5 **Bee-Matched address confirmed** (24/09/2026): `https://www.carenetconsultants.co.za/bee-matched`. Held in vercel/js/cnc-config.js as recruitmentPortalFutureUrl; recruitmentPortalUrl stays null (WhatsApp fallback) until the Director says the page is live, then it takes that value.

## 15. Amendment 7 (Director's instructions, 24/09/2026): File and Plan kept apart; sign off pricing for Care Net clients

15.1 **The File pages never lead into the Medical Surveillance Plan pages.** The Health and Safety File and the Risk Assessment application are a separate product from the Medical Surveillance Plan. On every File page (health-and-safety-file, hsf-builder, hsf-sample, hsf-staff, legislation): the Care Net logo links to /health-and-safety-file; company registration happens inside the builder (the signed out and no account states register the company in place through the existing /api/signon, with the same fields, validation and consent as the Plan landing), never by sending the visitor to /; the legislation link goes to /legislation; the "Build my Plan" button leaves the builder's help card. No File page links to /, /index.html, /pilot, /method, /shop, /sample, /medical-surveillance-plans, /industry, /assess or /account, except where a sentence names the Plan as a separate Care Net product in Section E. A test crawls every File page (static markup and, in Chromium, the rendered demonstration states) and fails on any such link. The portal, which joins both products, is exempt; its no account state still offers the File first ("Register my company for a File" goes to /hsf-builder.html, the Plan is a separate choice beside it), and a company registered for its File alone reads "Registered", never "Awaiting approval", because approval now comes only with the Plan (review findings F3, REG-R1 and REG-R2).

15.2 **Sign off pricing for Care Net clients** (replaces "market average less 25%"):
- A Care Net client's signed off File costs a flat **R4 000,00 excluding VAT** (one File, one site), whatever the industry market average.
- A company doing **more than 100 occupational medicals a year** with Care Net gets its signed off File **free** ("your File is on us").
- A **big site doing more than 500 medicals a year** with Care Net gets a free signed off File for the whole site **and for each of its subcontractors** working on that site.
- A sales executive verifies the declared medicals volume before a free tier is confirmed (the same rule as the Plan's CR-13.15), and multi site and above threshold construction work is still quoted by a sales executive.
- The market range and market average stay as researched; the Care Net client column shows R4 000,00; the tiers are explained beside the table. The figures and thresholds live in vercel/hsf/pricing.js (cnc_client_price 4000, free_medicals_threshold 100, site_and_subcontractors_threshold 500), not in page code. VAT treatment follows the existing rule (Care Net client prices exclude VAT) until Chantelle confirms otherwise.

15.3 **File only company registration (24/09/2026).** Registering from the builder never starts a Medical Surveillance Plan. The builder posts /api/signon with `source: 'hsf'`; api/signon.js (v1.5.0) then calls `hsf_client_register(p jsonb)` (migration 056 step 4, service role only) instead of `msp_client_signon`, whose `msp_client_start_assessment` approves the account and mints a Plan assessment token. `hsf_client_register` records the account as the Plan landing does (same fields and validation, lower case email match, a repeat only fills a missing contact number; audited `hsf_client_register` or `hsf_client_register_repeat`), never approves it and never touches `msp_form_access`; the answer carries status, reference, existing, company_name, contact_number and declined, never a token or assessment_url. Every other /api/signon rule holds, and the signed in contact rule is stricter: a File registration is made only for the caller whose confirmed, signed in email is the contact_email posted; anyone else gets 401 and nothing is looked up or recorded (review finding REG-R3). If the function is not in the database yet the answer is 503 "File registration is not available yet", never a fallback; any other source value is a 400; without source (the Plan landing) nothing changes. The File needs no approval (hsf_link_account, consent, generation, verification and uploads refuse only a declined account); the Plan starts for such a company only if its contact later chooses it (/api/company-lookup on the Plan landing). The tick box now reads "I agree that Care Net uses these details to register my company with Care Net, prepare my Health and Safety File and contact me about it. They are processed in line with POPIA." (15.1's "same consent" is read with this wording). Proved by test/sql/hsf_register_checks.sql and test/api/signon.test.js. Apply 056 before the builder's registration goes live; until then it answers 503.
